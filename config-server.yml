server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AbdallahIsmaili/microservices-config
          clone-on-start: true
          default-label: main
          basedir: /tmp/config-repo
          force-pull: true
